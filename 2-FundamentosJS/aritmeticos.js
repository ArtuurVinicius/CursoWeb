const [a, b, c, d] = [3, 5, 1, 7]

const soma = a + b + c + d
const subtracao = d - b
const multi = a * b
const divisao = d / a
const modulo = a % 2
// d++  post fixed
// op1 + op2  in fixed
console.log(soma, subtracao, multi, divisao, modulo)